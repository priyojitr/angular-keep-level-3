import { NotstartedPipe } from './notstarted.pipe';

describe('NotstartedPipe', () => {
  it('create an instance', () => {
    const pipe = new NotstartedPipe();
    expect(pipe).toBeTruthy();
  });
});
